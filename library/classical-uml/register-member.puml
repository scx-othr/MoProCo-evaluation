@startuml
title Library::registerMember(member : Member)

start
if (self.members->exists(m | m.memberId = member.memberId)) then ([true])
  :raise("Member ID is already registered.");
  stop
else ([false])
  :self.members := self.members->including(member);
  stop
endif
@enduml
