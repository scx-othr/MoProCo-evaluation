@startuml
title Library::registerMember(m : Member)

start
if (self.members->exists(x | x.memberId = m.memberId)) then ([true])
  :raise("Member ID is already registered.");
  stop
else ([false])
  :self.members := self.members->including(m);
  stop
endif
@enduml
