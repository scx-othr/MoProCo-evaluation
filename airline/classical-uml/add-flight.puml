@startuml
title Airline::addFlight(flight : Flight)

start
if (self.flights->exists(f | f.flightNumber = flight.flightNumber)) then ([true])
  :raise("Flight already exists");
  stop
else ([false])
  :self.flights := flights->including(flight);
  stop
endif
@enduml