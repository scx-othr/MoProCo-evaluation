@startuml
title Airline::addFlight(f : Flight)

start
if (self.flights->exists(x | x.flightNumber = f.flightNumber)) then ([true])
  :raise("Flight already exists");
  stop
else ([false])
  :self.flights := flights->including(f);
  stop
endif
@enduml