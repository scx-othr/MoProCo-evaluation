@startuml
title Flight::addSeat(s : Seat)

start
if (self.seats->exists(x | x.seatNumber = s.seatNumber)) then ([true])
  stop
else ([false])
  :self.seats := self.seats->including(s);
  :s.status := SeatStatus::Available;
  stop
endif
@enduml
