@startuml
title Flight::addSeat(seat : Seat)

start
if (self.seats->exists(s | s.seatNumber = seat.seatNumber)) then ([true])
  stop
else ([false])
  :self.seats := self.seats->including(seat);
  :seat.status := SeatStatus::Available;
  stop
endif
@enduml
